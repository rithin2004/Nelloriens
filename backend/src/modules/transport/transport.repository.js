import { FirestoreRepo } from '../../utils/firestoreRepo.js'
export const transportRepo         = new FirestoreRepo('transport')
export const transportCategoriesRepo = new FirestoreRepo('transport_categories')
